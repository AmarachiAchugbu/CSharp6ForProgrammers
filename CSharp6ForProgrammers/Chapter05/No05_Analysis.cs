// Listing 5.5: Analysis.cs
// Analysis of examination results, using nested control statements.
using System;

class No05_Analysis
{
    //static void Main()
    //{
    //    // Initialization phase: initialize variables in declarations
    //    int studentCounter = 0; // counts the number of students results entered
    //    int passCount = 0, failCount = 0; // counts the number of passes and fails
    //    int result; // stores the result of each student

    //    // Processes 10 students using counter-controlled iteration
    //    while (studentCounter < 10)
    //    {
    //        // Prompt the user for input and obtain a value from the user
    //        Console.Write("Enter result (1 = pass, 2 = fail): ");
    //        result = int.Parse(Console.ReadLine());

    //        if (result == 1)
    //        {
    //            passCount++; // increment passes
    //        }
    //        else if (result == 2)
    //        {
    //            failCount++; // increment failures
    //        }

    //        studentCounter++; // increments the students for eventual loop termination
    //    }

    //    // Summarize the results
    //    Console.WriteLine($"\nPassed: {passCount}\nFailed: {failCount}");

    //    // Determine whether more than 8 students passed
    //    if (passCount > 8)
    //    {
    //        Console.WriteLine("Bonus to instructor!");
    //    }
    //}
}
